% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_simulate.R
\name{simulate_parallel}
\alias{simulate_parallel}
\title{Parallel simulation of a Whittle-Matérn field on a metric graph}
\usage{
simulate_parallel(graph, ..., n_cores = NULL, cluster = NULL)
}
\arguments{
\item{graph}{A \code{metric_graph} object.}

\item{...}{Further arguments passed to \code{simulate.metric_graph()}, including
\code{alpha}, \code{method}, \code{kappa}/\code{tau} or \code{range}/\code{sigma}, \code{PtE}, \code{type},
\code{BC}, \code{nsim}, and \code{seed}.}

\item{n_cores}{Number of parallel workers.}

\item{cluster}{An already-registered \code{doParallel} cluster.}
}
\value{
Numeric vector (nsim = 1) or matrix with nsim columns.
}
\description{
Calls \code{simulate.metric_graph()} with \code{parallel = TRUE}.  The edge loop is
distributed over a \code{doParallel}/\code{foreach} cluster.
}
\examples{
\donttest{
V <- rbind(c(0,0), c(1,0), c(1,1), c(0,1))
E <- rbind(c(1,2), c(2,3), c(3,4), c(4,1))
g <- metric_graph$new(V = V, E = E)
t_norm <- seq(0.1, 0.9, by = 0.2)
PtE    <- do.call(rbind, lapply(1:4, function(e) cbind(e, t_norm)))
u <- simulate_parallel(g, alpha = 1, method = "kriging",
                       kappa = 1, tau = 1, PtE = PtE, n_cores = 2)
}
}
\seealso{
\code{\link[=sample_spde]{sample_spde()}}
}
