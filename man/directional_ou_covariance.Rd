% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariance_directional_ou.R
\name{directional_ou_covariance}
\alias{directional_ou_covariance}
\title{Closed-form covariance of the directional OU model}
\usage{
directional_ou_covariance(
  graph,
  kappa,
  tau,
  PtE = NULL,
  PtE2 = NULL,
  sigma_source = NULL,
  normalized = TRUE,
  cpp = TRUE
)
}
\arguments{
\item{graph}{A \code{metric_graph} object with directional edge weights set
(\code{set_edge_weights(directional_weights = ...)}) and the vertex weight
functions set (\code{setDirectionalWeightFunction()}).}

\item{kappa}{Rate parameter of the OU process (\code{kappa > 0}).}

\item{tau}{White-noise scale. The stationary marginal variance is
\code{1/(2*kappa*tau^2)} -- the same convention as \code{MetricGraph:::r_1()}. Note
this is NOT \code{reciprocal_tau} (used elsewhere in the package as
\code{tau = 1/reciprocal_tau}); pass \code{tau} directly here.}

\item{PtE}{Evaluation points, a matrix with columns \verb{(edge_number, distance_on_edge)}. Defaults to \code{graph$get_PtE()} (the observation
locations).}

\item{PtE2}{Second set of evaluation points for the cross-covariance
\code{Cov(u(PtE), u(PtE2))}. Defaults to \code{PtE}, giving the symmetric
\verb{n x n} covariance matrix at the observation locations.}

\item{sigma_source}{Anchoring variance at the source vertices
(indegree 0). \code{NULL} (default) anchors every source at the stationary
variance \code{1/(2*kappa*tau^2)}, matching
\code{Qalpha1_edges_custom(..., stationary_points = "all")}. Otherwise a
numeric vector giving \code{Var(u)} at each source vertex, either named by
vertex index (as character) or positional in the order
\code{which(graph$get_degrees("indegree") == 0)}.}

\item{normalized}{If \code{TRUE} (default), \code{distance_on_edge} in \code{PtE}/\code{PtE2}
is in \verb{[0, 1]}; if \code{FALSE}, distances are absolute.}

\item{cpp}{If \code{TRUE} (default), use the C++ numeric setup and the shared C++
covariance fill for oriented in-trees and out-trees. Irregular trees use
the generic R fallback. Set to \code{FALSE} to use the pure-R implementation
throughout.}
}
\value{
A dense \code{matrix} with \code{nrow(PtE)} rows and \code{nrow(PtE2)} columns.
}
\description{
Evaluates the covariance function of the "proper global OU" directional
model (Whittle-Matern alpha = 1 with directional vertex conditions) in
closed form, via the last-common-ancestor (LCA) representation, without
assembling or inverting the precision matrix.
}
\details{
The covariance between two points \code{s} and \code{t} is
\deqn{r(s,t) = r(a,a) \, A(a,s) \, A(a,t)}
where \code{a} is the last common ancestor (LCA) of \code{s} and \code{t} (or covariance
\code{0} if no common ancestor exists), \code{r(a,a)} is the marginal variance at
\code{a}, and
\deqn{A(x,y) = \exp(-\kappa \, d(x,y)) \prod_v \beta_v}
is the transfer factor for the (unique, forward) path from \code{x} to \code{y},
the product running over the \code{beta_v} vertex weights of every vertex
crossed strictly between \code{x} and \code{y}.

In plain terms: the last common ancestor of two points is the
furthest-downstream point that is upstream of both of them (it does not
exist if the two points lie on disjoint headwater branches, in which case
their covariance is exactly zero). The transfer factor \code{A(x,y)} is how
much of the value at \code{x} propagates forward to \code{y}: exponential decay
over the distance \code{d(x,y)}, attenuated by the directional weight
\code{beta_v} at every vertex the path passes through.

This closed form requires \code{graph} to represent a directed \emph{tree}: the
undirected skeleton must have no cycles, and the directed graph must
itself be acyclic. If \code{graph} has a directed cycle, is not a tree, or has
no directional weight function set, this errors (via the internal
\code{directional_ou_setup()} validation) -- see
\code{graph$setDirectionalWeightFunction()} to set one.
}
\examples{
edge1 <- rbind(c(0, 0), c(1, 0))
edge2 <- rbind(c(1, 0), c(2, 0))
edge3 <- rbind(c(1, 0), c(1, 1))
graph <- metric_graph$new(edges = list(edge1, edge2, edge3))
graph$set_edge_weights(weights = data.frame(w = c(1, 1, 1)),
                       directional_weights = "w")
graph$setDirectionalWeightFunction(f_in = function(x) sqrt(x / sum(x)))
Sigma <- directional_ou_covariance(graph, kappa = 1, tau = 1,
                                   PtE = rbind(c(1, 0.5), c(2, 0.5)))
Sigma
}
\seealso{
\code{\link[=directional_ou_variance]{directional_ou_variance()}} for the diagonal only.
}
