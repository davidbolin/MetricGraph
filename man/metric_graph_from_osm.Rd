% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osm.R
\name{metric_graph_from_osm}
\alias{metric_graph_from_osm}
\title{Build a metric_graph directly from an OpenStreetMap query}
\usage{
metric_graph_from_osm(
  bbox,
  key = "highway",
  value = NULL,
  endpoint = "https://overpass-api.de/api/interpreter",
  timeout = 180,
  user_agent = "MetricGraph-R-package",
  cache_path = NULL,
  retries = 2,
  ...
)
}
\arguments{
\item{bbox}{A bounding box, passed to \code{\link[osmdata:opq]{osmdata::opq()}}. May be a
length-4 numeric vector \code{c(xmin, ymin, xmax, ymax)}, an \code{sf::bbox}
object, or any other form accepted by \code{\link[osmdata:opq]{osmdata::opq()}}.}

\item{key}{OSM key to filter on, e.g. \code{"highway"}. Passed to
\code{\link[osmdata:add_osm_feature]{osmdata::add_osm_feature()}}. Use \code{NULL} to skip the feature
filter and fetch everything in the bbox.}

\item{value}{Optional character vector of values for \code{key}, e.g.
\code{c("motorway", "motorway_link")}.}

\item{endpoint}{Overpass endpoint URL, or a character vector of URLs
that are tried in order. Defaults to the main \code{overpass-api.de}
endpoint. Mirror URLs (e.g.
\verb{https://overpass.kumi.systems/api/interpreter}) work too.}

\item{timeout}{Server-side query timeout in seconds. Forwarded to
both \code{\link[osmdata:opq]{osmdata::opq()}} and the HTTP request.}

\item{user_agent}{String sent as the \code{User-Agent} HTTP header.}

\item{cache_path}{Optional path. If provided and the file already
exists, the download is skipped and the cached response is parsed
instead. If provided and the file does not exist, the response is
saved there for re-use.}

\item{retries}{Number of times a request to each endpoint is retried
after a transient failure (HTTP 429 or 5xx, or a connection error),
with exponential backoff between attempts.}

\item{...}{Forwarded to \link{metric_graph}'s \verb{$new()}. Use this to
pass \code{tolerance}, \code{perform_merges}, \code{check_connected},
\code{which_longlat}, etc.}
}
\value{
A \link{metric_graph} object built from the OSM ways.
}
\description{
Convenience wrapper that fetches OSM data with \code{\link[=fetch_osm]{fetch_osm()}} and
builds a \link{metric_graph} from the returned linestrings in one call.
\code{longlat = TRUE} is set automatically because OSM coordinates are
always in WGS84 latitude / longitude. Other graph-construction
arguments (\code{tolerance}, \code{perform_merges}, ...) are forwarded to
\link{metric_graph}'s \verb{$new()} method without overriding its defaults.
}
\examples{
\dontrun{
## Build a freeway graph for a small bbox around San Jose, splitting
## at intersections via the edge_edge tolerance.
g <- metric_graph_from_osm(
  bbox           = c(-122.10, 37.22, -121.80, 37.45),
  value          = c("motorway", "motorway_link"),
  perform_merges = TRUE,
  tolerance      = list(edge_edge = 1e-5)
)
g$plot()
}

}
\seealso{
\code{\link[=fetch_osm]{fetch_osm()}} for the lower-level fetch + parse step.
}
