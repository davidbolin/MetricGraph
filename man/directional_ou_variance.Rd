% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariance_directional_ou.R
\name{directional_ou_variance}
\alias{directional_ou_variance}
\title{Closed-form marginal variance of the directional OU model}
\usage{
directional_ou_variance(
  graph,
  kappa,
  tau,
  PtE = NULL,
  sigma_source = NULL,
  normalized = TRUE,
  cpp = TRUE
)
}
\arguments{
\item{graph}{A \code{metric_graph} object with directional edge weights set
(\code{set_edge_weights(directional_weights = ...)}) and the vertex weight
functions set (\code{setDirectionalWeightFunction()}).}

\item{kappa}{Rate parameter of the OU process (\code{kappa > 0}).}

\item{tau}{White-noise scale. The stationary marginal variance is
\code{1/(2*kappa*tau^2)} -- the same convention as \code{MetricGraph:::r_1()}. Note
this is NOT \code{reciprocal_tau} (used elsewhere in the package as
\code{tau = 1/reciprocal_tau}); pass \code{tau} directly here.}

\item{PtE}{Evaluation points, a matrix with columns \verb{(edge_number, distance_on_edge)}. Defaults to \code{graph$get_PtE()} (the observation
locations).}

\item{sigma_source}{Anchoring variance at the source vertices
(indegree 0). \code{NULL} (default) anchors every source at the stationary
variance \code{1/(2*kappa*tau^2)}, matching
\code{Qalpha1_edges_custom(..., stationary_points = "all")}. Otherwise a
numeric vector giving \code{Var(u)} at each source vertex, either named by
vertex index (as character) or positional in the order
\code{which(graph$get_degrees("indegree") == 0)}.}

\item{normalized}{If \code{TRUE} (default), \code{distance_on_edge} in \code{PtE}/\code{PtE2}
is in \verb{[0, 1]}; if \code{FALSE}, distances are absolute.}

\item{cpp}{If \code{TRUE} (default), use the C++ numeric setup and the shared C++
covariance fill for oriented in-trees and out-trees. Irregular trees use
the generic R fallback. Set to \code{FALSE} to use the pure-R implementation
throughout.}
}
\value{
A numeric vector of length \code{nrow(PtE)}.
}
\description{
\code{r(x,x)} at each evaluation point -- the diagonal of
\code{\link[=directional_ou_covariance]{directional_ou_covariance()}}, computed directly via the marginal
variance recursion along each edge, without any last-common-ancestor or
transfer-factor work.
}
\examples{
edge1 <- rbind(c(0, 0), c(1, 0))
edge2 <- rbind(c(1, 0), c(2, 0))
edge3 <- rbind(c(1, 0), c(1, 1))
graph <- metric_graph$new(edges = list(edge1, edge2, edge3))
graph$set_edge_weights(weights = data.frame(w = c(1, 1, 1)),
                       directional_weights = "w")
graph$setDirectionalWeightFunction(f_in = function(x) sqrt(x / sum(x)))
directional_ou_variance(graph, kappa = 1, tau = 1,
                        PtE = rbind(c(1, 0.5), c(2, 0.5)))
}
\seealso{
\code{\link[=directional_ou_covariance]{directional_ou_covariance()}} for the full closed-form
covariance (including the \verb{\deqn{r(s,t) = r(a,a) A(a,s) A(a,t)}} formula
and the tree/acyclicity requirements).
}
