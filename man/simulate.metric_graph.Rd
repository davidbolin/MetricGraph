% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_simulate.R
\name{simulate.metric_graph}
\alias{simulate.metric_graph}
\title{Simulate a Whittle-Matérn field on a metric graph}
\usage{
\method{simulate}{metric_graph}(
  object,
  nsim = 1,
  seed = NULL,
  alpha = 1,
  method = c("direct", "kriging", "extended"),
  impl = c("cpp", "R"),
  kappa,
  tau,
  range,
  sigma,
  PtE = NULL,
  type = "manual",
  BC = 1,
  parallel = FALSE,
  n_cores = NULL,
  cluster = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{metric_graph} object.}

\item{nsim}{Number of samples.  Returns a vector (nsim = 1) or matrix
(nsim > 1) of sampled values.}

\item{seed}{Integer seed for \code{set.seed()} before the vertex draw.  Pass
\code{NULL} (default) to use the current RNG state.}

\item{alpha}{Smoothness parameter (1 or 2).}

\item{method}{Simulation algorithm: \code{"direct"} (Method A, \eqn{O(m^3)}),
\code{"kriging"} (Method B, \eqn{O(m)}), or \code{"extended"} (add simulation
locations as graph vertices, single sparse Cholesky draw).}

\item{impl}{Implementation: \code{"cpp"} (default, Rcpp/Eigen) or \code{"R"} (pure
R reference, for testing/comparison).  Ignored for \code{method = "extended"}.}

\item{kappa}{Range parameter.}

\item{tau}{Precision parameter.}

\item{range}{Practical correlation range (alternative to \code{kappa}/\code{tau}).}

\item{sigma}{Marginal standard deviation (alternative to \code{kappa}/\code{tau}).}

\item{PtE}{Matrix with columns \verb{(edge_number, normalised_position)}.
Required when \code{type = "manual"} (the default).}

\item{type}{Location specification: \code{"manual"} (use \code{PtE}), \code{"mesh"} (mesh
nodes), or \code{"obs"} (observation locations).}

\item{BC}{Boundary condition for degree-1 vertices: 1 = stationary
(default), 0 = Neumann.}

\item{parallel}{Logical.  Use a parallel cluster for the edge loop?}

\item{n_cores}{Number of parallel workers.  Ignored if \code{cluster} is
supplied.  Defaults to \code{detectCores() - 1}.}

\item{cluster}{An already-registered \code{doParallel} cluster.  If \code{NULL} and
\code{parallel = TRUE}, a cluster is created and stopped automatically.}

\item{...}{Ignored.}
}
\value{
Numeric vector (nsim = 1) or matrix with nsim columns of field
values at the requested locations, ordered to match \code{PtE}.
}
\description{
Draws unconditional (prior) samples of a Whittle-Matérn field on a metric
graph using one of two exact algorithms from Section 6.6 of the paper.
}
\details{
\strong{Algorithm A ("direct")}: interior values on each edge are drawn from the
bridge conditional \eqn{\mathcal{N}(S_e b_e, \Sigma^*_e)} via a dense
Cholesky of the \eqn{m \times m} bridge covariance.  Cost: \eqn{O(m^3)}
per edge.  Preferred when \eqn{m} is small (roughly \eqn{m \lesssim 100}).

\strong{Algorithm B ("kriging")}: an unconditioned Matérn process is simulated on
each edge by a linear Markov recursion, then the bridge boundary data are
imposed by a kriging correction (Theorem 2 / Proposition 4).  Cost:
\eqn{O(m)} per edge.  Preferred for large \eqn{m}.

Both algorithms produce draws from exactly the same finite-dimensional
distribution; they differ only in computational cost.

The two-step procedure is:
\enumerate{
\item Draw the vertex (boundary) state from the vertex precision via a
sparse Cholesky.
\item For each edge (independently given the vertex state), draw the
interior values using the chosen algorithm.
}

When \code{parallel = TRUE} the edge loop is distributed over a
\code{doParallel}/\code{foreach} cluster.  Reproducibility requires passing a \code{seed}
so that per-edge seeds are derived deterministically.
}
\examples{
# Small square graph, alpha = 1
V <- rbind(c(0,0), c(1,0), c(1,1), c(0,1))
E <- rbind(c(1,2), c(2,3), c(3,4), c(4,1))
g <- metric_graph$new(V = V, E = E)
t_norm <- seq(0.1, 0.9, by = 0.2)
PtE    <- do.call(rbind, lapply(1:4, function(e) cbind(e, t_norm)))
u <- simulate(g, alpha = 1, method = "direct", kappa = 1, tau = 1, PtE = PtE)
\donttest{
# Method B on a larger graph, parallel
u2 <- simulate(g, alpha = 2, method = "kriging", range = 1, sigma = 1,
               PtE = PtE, parallel = TRUE, n_cores = 2)
}
}
\seealso{
\code{\link[=sample_spde]{sample_spde()}}
}
